// Legacy sample file helper removed. This module now only keeps a loud warning.
export const initSampleFile = () => {
  console.warn('initSampleFile is deprecated and no longer available.');
};

export default { initSampleFile };
