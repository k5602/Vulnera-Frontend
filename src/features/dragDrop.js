// Legacy drag-and-drop initializer removed. This module is kept to avoid breaking imports.
export const initDragAndDrop = () => {
  console.warn('initDragAndDrop is deprecated and no longer does anything.');
};

export default { initDragAndDrop };
