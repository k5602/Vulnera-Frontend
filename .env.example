# Vulnera Frontend — Example Environment Variables
# Copy this file to `.env` and customize values for your environment.
# Only variables prefixed with VITE_ are exposed to the browser by Vite.
#
# Precedence in src/config.js:
# 1) VITE_* at build time  2) window.VULNERA_* at runtime  3) process.env  4) defaults
#
# Notes:
# - All values are strings. For booleans use: true or false (without quotes).
# - API_TIMEOUT is a number in milliseconds.
# - ENVIRONMENT is informational; use `--mode` for build/preview variants if desired.
# - To initialize quickly: `npm run env:init` (copies this to .env)

###############################
# Core App Settings
###############################

# Public-facing app name and version (used in headers and UI)
VITE_APP_NAME=Vulnera
VITE_APP_VERSION=1.0.0

# Enable extra logging in the browser console when true
# In production set this to false
VITE_ENABLE_DEBUG=true

# Environment label used by the UI and config (development|staging|production)
VITE_ENVIRONMENT=development

###############################
# API Configuration
###############################

# Base URL of the backend API (no trailing slash)
# For local dev the backend typically runs on 3000
VITE_API_BASE_URL=http://localhost:3000

# API version path segment appended to API_BASE_URL (e.g., /api/v1)
VITE_API_VERSION=v1

# HTTP timeout for API requests in milliseconds
# Increase for slow networks or large reports
VITE_API_TIMEOUT=30000

###############################
# Security: Allowed origins for API_BASE_URL
###############################
# The frontend validates API_BASE_URL against an allowlist to avoid misconfigurations.
# Provide a comma-separated list of patterns. Supported formats:
# 1) Full URLs with optional wildcards (*)   e.g., https://api.example.com, https://*.example.com
# 2) Raw regex (start ^ or end $)            e.g., ^http://localhost:\d+$
# 3) Domain-only patterns (optional port)    e.g., *.example.com, api.example.com
#
# Examples below include localhost and known Vulnera hosts. Adjust to your environments.
# IMPORTANT: Keep localhost during development, lock down for staging/production.

VITE_ALLOWED_ORIGINS=https://api.example.com, https://*.example.com

###############################
# Staging Example (uncomment and edit)
###############################
# VITE_ENVIRONMENT=staging
# VITE_ENABLE_DEBUG=true
# VITE_API_BASE_URL=https://staging-api.your-domain.tld
# VITE_API_VERSION=v1
# VITE_API_TIMEOUT=60000
# VITE_ALLOWED_ORIGINS=https://staging-api.your-domain.tld, https://*.your-domain.tld

###############################
# Production Example (uncomment and edit)
###############################
# VITE_ENVIRONMENT=production
# VITE_ENABLE_DEBUG=false
# VITE_API_BASE_URL=https://api.your-domain.tld
# VITE_API_VERSION=v1
# VITE_API_TIMEOUT=60000
# VITE_ALLOWED_ORIGINS=https://api.your-domain.tld, https://*.your-domain.tld

###############################
# Tips
###############################
# - You can override at runtime using public/runtime-config.js:
#     window.VulneraRuntimeConfig.setConfig({ apiBaseUrl: 'https://api.example.com', ... })
# - Vite supports mode-specific files: .env.development, .env.staging, .env.production
# - After editing .env, restart the dev server or rebuild for changes to take effect.
